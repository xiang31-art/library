#include <stdio.h>
#include <string.h>

/*
========================================
構造体 超基本練習
========================================
1つずつゆっくり進めていきましょう！
*/

// ========================================
// ステップ1: 構造体を定義する
// ========================================
// 本の情報を管理する構造体
// タイトル(title)と価格(price)を持つ
struct Book {
    char title[50];  // タイトル
    int price;       // 価格
};

int main() {
    printf("=== 構造体 超基本練習 ===\n\n");
    
    // ========================================
    // ステップ2: 構造体変数を作る
    // ========================================
    printf("--- ステップ2: 構造体変数を作る ---\n");
    
    // 例: 本の情報を作る
    struct Book book1;
    
    // メンバに値を入れる
    strcpy(book1.title, "C言語入門");
    book1.price = 2000;
    
    // 表示してみる
    printf("本のタイトル: %s\n", book1.title);
    printf("価格: %d円\n\n", book1.price);
    
    // ========================================
    // 練習1: あなたもやってみよう！
    // ========================================
    printf("--- 練習1: 構造体変数を作ってみよう ---\n");
    
    // book2という変数を作ってください
    // ここにコードを書いてください
    struct Book book2;
    
    
    // book2のタイトルを"Python入門"にしてください
    // ここにコードを書いてください
    strcpy(book2.title, "Python入門");
    
    
    // book2の価格を2500にしてください
    // ここにコードを書いてください
    book2.price = 2500;
    
    
    // 表示（これは書いてあります）
    printf("本のタイトル: %s\n", book2.title);
    printf("価格: %d円\n\n", book2.price);
    
    // ========================================
    // ステップ3: 初期化しながら作る（簡単な方法）
    // ========================================
    printf("--- ステップ3: 初期化しながら作る ---\n");
    
    // 例: 作ると同時に値を入れる
    struct Book book3 = {"Java入門", 3000};
    
    printf("本のタイトル: %s\n", book3.title);
    printf("価格: %d円\n\n", book3.price);
    
    // ========================================
    // 練習2: 初期化しながら作ってみよう！
    // ========================================
    printf("--- 練習2: 初期化しながら作ってみよう ---\n");
    
    // book4を作って、タイトル"Ruby入門"、価格2800で初期化
    // ヒント: struct Book book4 = {"タイトル", 価格};
    // ここにコードを書いてください
    struct Book book4 = {"Ruby入門",2800};
    
    
    // 表示
    printf("本のタイトル: %s\n", book4.title);
    printf("価格: %d円\n\n", book4.price);
    
    // ========================================
    // ステップ4: メンバの値を変更する
    // ========================================
    printf("--- ステップ4: メンバの値を変更する ---\n");
    
    struct Book book5 = {"Go入門", 2200};
    printf("変更前: %s, %d円\n", book5.title, book5.price);
    
    // 価格を変更
    book5.price = 1800;
    printf("変更後: %s, %d円\n\n", book5.title, book5.price);
    
    // ========================================
    // 練習3: 値を変更してみよう！
    // ========================================
    printf("--- 練習3: 値を変更してみよう ---\n");
    
    struct Book book6 = {"Rust入門", 3500};
    printf("変更前: %s, %d円\n", book6.title, book6.price);
    
    // book6の価格を3000に変更してください
    // ここにコードを書いてください
    book6.price = 3000;
    
    
    printf("変更後: %s, %d円\n\n", book6.title, book6.price);
    
    // ========================================
    // ステップ5: 2つの構造体を比較する
    // ========================================
    printf("--- ステップ5: 2つの構造体を比較する ---\n");
    
    struct Book bookA = {"HTML入門", 1500};
    struct Book bookB = {"CSS入門", 1800};
    
    // どちらが高いか比較
    if(bookA.price > bookB.price) {
        printf("%s の方が高い\n\n", bookA.title);
    } else {
        printf("%s の方が高い\n\n", bookB.title);
    }
    
    // ========================================
    // 練習4: 比較してみよう！
    // ========================================
    printf("--- 練習4: 比較してみよう ---\n");
    
    struct Book bookX = {"JavaScript入門", 2400};
    struct Book bookY = {"TypeScript入門", 2600};
    
    // bookXとbookYの価格を比較して、安い方のタイトルを表示
    // ここにコードを書いてください
    if(bookX.price < bookY.price){
        printf("%sのほうが安い\n",bookX.title);
    }
    else{
        printf("%sのほうが安い\n",bookY.title);
    }
    
    
    
    
    
    printf("\n完成です！お疲れ様でした！\n");
    
    return 0;
}

/*
========================================
ヒント:
========================================

練習1:
struct Book book2;
strcpy(book2.title, "Python入門");
book2.price = 2500;

練習2:
struct Book book4 = {"Ruby入門", 2800};

練習3:
book6.price = 3000;

練習4:
if(bookX.price < bookY.price) {
    printf("%s の方が安い\n", bookX.title);
} else {
    printf("%s の方が安い\n", bookY.title);
}

========================================
覚えること:
========================================
1. 構造体の定義: struct 名前 { メンバ; };
2. 変数を作る: struct 名前 変数名;
3. メンバにアクセス: 変数名.メンバ名
4. 初期化: struct 名前 変数名 = {値1, 値2};
*/
